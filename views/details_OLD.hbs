<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Attach Accessory</title>
    <link href="./css/site.css" type="text/css" rel="stylesheet">
    <link rel='shortcut icon' type='image/x-icon' href='./images/favicon.png' />
</head>
<body>
    <div class="container">
        <header>
            {{> navigation}}
        </header>
      <div class="row">
        <div class="col-8"><h1>{{cube.name}} </h1>
              <div class="details">
        <p><span>Description:</span> {{cube.description}}</p>
        <p><span>Difficulty level:</span> {{cube.level}}</p>
          <a class="btn btn-primary" href="/attach/{{cube._id}}">Attach</a>
          {{#if loggedIn}}
          <a class="btn btn-info" href="/edit/{{cube._id}}">Edit</a>
          <a class="btn btn-danger" href="/delete/{{cube._id}}">Delete</a>
          {{/if}}
            </div>
            <h2>Accessories</h2>
            <div class="accessories">
            {{#if cube.accessories}}
              {{#each cube.accessories }}

              <div class="card" style="width: 18rem;">
               <img src="{{imageUrl}}" class="card-img-top" alt="{{name}}">
               <div class="card-body">
                <h5 class="card-title">{{name}}</h5>
               <p class="card-text">{{description}}</p>
                  <a href="#" class="btn btn-primary">Go somewhere</a>
                </div>
              </div>
                {{/each}}
              {{else}}
                <h3 class="italic">This cube has no accessories yet...</h3>
              {{/if}}




        <div class="col"><img class="cube" src="{{cube.image_url}}"></div>
      



      </div>



    </div>
    <footer>@Cubicle: Exercise for Express.js and Handlebars</footer>
</body>
</html>